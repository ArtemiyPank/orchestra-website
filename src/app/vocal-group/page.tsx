import VocalGroupClient from "./VocalGroupClient"

export const metadata = {
  title: "Vocal Group | Atid Raziel Orchestra",
  description: "The vocal group of the Atid Raziel School Orchestra — leader, soloists, and featured songs.",
}

const VocalGroupPage = () => {
  return <VocalGroupClient />
}

export default VocalGroupPage
